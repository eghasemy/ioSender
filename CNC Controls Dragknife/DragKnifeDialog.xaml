<Window x:Class="CNC.Controls.DragKnife.DragKnifeDialog"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="using:CNC.Controls;assembly=CNC.Controls.WPF"
             xmlns:local="using:CNC.Controls.DragKnife"
             mc:Ignorable="d" 
             d:DesignHeight="320" d:DesignWidth="300"
             SizeToContent="WidthAndHeight" ResizeMode="NoResize"
            
             Title="Drag knife parameters" Icon="App.ico">
    <Grid Background="#FFE5E5E5">
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Height="Auto" Margin="5,5">
            <HeaderedContentControl Header="Knife" HorizontalAlignment="Left" Margin="5,5,0,0" VerticalAlignment="Top" Height="Auto" Width="280">
                <StackPanel>
                    <Controls:NumericField Label="Tip offset:" ColonAt="120" Unit="mm" Format="#0.###" Value="{Binding Path=KnifeTipOffset, Mode=TwoWay}"/>
                    <Controls:NumericField Label="Cut depth:" ColonAt="120" Unit="mm" Format="-###0.###" Value="{Binding Path=CutDepth, Mode=TwoWay}"/>
                    <Controls:NumericField Label="Swivel angle:" ColonAt="120" Unit="deg" Format="##0" Value="{Binding Path=SwivelAngle, Mode=TwoWay}"/>
                    <Controls:NumericField Label="Dent length:" ColonAt="120" Unit="mm" Format="###0.###" Value="{Binding Path=DentLength, Mode=TwoWay}"/>
                </StackPanel>
            </HeaderedContentControl>
            <HeaderedContentControl Header="Retract" HorizontalAlignment="Left" Margin="5,5,0,0" VerticalAlignment="Top" Height="Auto" Width="280">
                <StackPanel>
                    <CheckBox Content="Retract enable" IsChecked="{Binding Path=RetractEnable, Mode=TwoWay}" Margin="120,5,0,0"/>
                    <Controls:NumericField Label="Retract angle:" ColonAt="120" Unit="deg" Format="##0" Value="{Binding Path=RetractAngle, Mode=TwoWay}"/>
                    <Controls:NumericField Label="Retract depth:" ColonAt="120" Unit="mm/min" Format="-###0.###" Value="{Binding Path=RetractDepth, Mode=TwoWay}"/>
                </StackPanel>
            </HeaderedContentControl>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Width="Auto" Margin="0,10">
                <Button x:Name="btnOk" Content="Ok" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75"/>
                <Button x:Name="btnCancel" Content="Cancel" HorizontalAlignment="Left" Margin="5,0,0,0" VerticalAlignment="Top" Width="75"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
