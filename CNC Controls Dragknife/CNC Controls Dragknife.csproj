<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>CNC.Controls.Dragknife</RootNamespace>
    <AssemblyName>CNC.Controls.Dragknife</AssemblyName>
    <UICulture>en-US</UICulture>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)'=='Debug'">
    <DefineConstants>TRACE;DEBUG</DefineConstants>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)'=='Release'">
    <DefineConstants>TRACE</DefineConstants>
  </PropertyGroup>

  <!-- Project references -->
  <ItemGroup>
    <ProjectReference Include="..\CNC Controls\CNC Controls\CNC Controls.csproj" />
    <ProjectReference Include="..\CNC Core\CNC Core\CNC Core.csproj" />
  </ItemGroup>

  <!-- Remove the default Compile items to avoid duplicates -->
  <ItemGroup>
    <Compile Remove="**/*.cs" />
    <Page Remove="**/*.xaml" />
    <Resource Remove="**/*" />
  </ItemGroup>

  <!-- For cross-platform build, include core dragknife logic -->
  <ItemGroup>
    <Compile Include="DragknifeViewModel.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
  </ItemGroup>

</Project>